# 页面修复说明

## ✅ 已创建的页面

### 1. 订单页面
**路径**: `/orders`  
**文件**: `frontend/src/pages/Orders.vue`  
**功能**: 
- 展示用户的所有交易订单
- Tab切换：全部/持仓中/已平仓
- 显示订单详情：品种、数量、价格、盈亏
- 下拉刷新和加载更多

### 2. 银行卡管理
**路径**: `/bank-cards`  
**文件**: `frontend/src/pages/BankCards.vue`  
**功能**:
- 展示用户的银行卡列表
- 添加新银行卡
- 删除银行卡
- 卡号脱敏显示

### 3. 系统通知
**路径**: `/notifications`  
**文件**: `frontend/src/pages/Notifications.vue`  
**功能**:
- 展示系统通知列表
- 标记单条通知已读
- 全部标记已读
- 未读通知高亮显示

### 4. 关于页面
**路径**: `/about`  
**文件**: `frontend/src/pages/About.vue`  
**功能**:
- 公司信息展示
- 联系方式（电话、微信）
- 用户协议、隐私政策、风险提示入口
- 版权信息

### 5. 用户管理（管理员）
**路径**: `/admin/users`  
**文件**: `frontend/src/pages/admin/Users.vue`  
**功能**:
- 查看所有用户
- 搜索用户（按手机号）
- Tab筛选：全部/待审核/已激活/已禁用
- 审核用户（通过/拒绝）
- 显示用户详细信息

**权限要求**: `super_admin` 或 `support`

### 6. 充值管理（管理员）
**路径**: `/admin/deposits`  
**文件**: `frontend/src/pages/admin/Deposits.vue`  
**功能**:
- 查看充值申请
- Tab切换：待审核/已通过/已拒绝
- 审核充值申请（通过/拒绝）
- 显示申请详情和用户信息

**权限要求**: `super_admin` 或 `support`

### 7. 提现管理（管理员）
**路径**: `/admin/withdraws`  
**文件**: `frontend/src/pages/admin/Withdraws.vue`  
**功能**:
- 查看提现申请
- Tab切换：待审核/已通过/已拒绝
- 审核提现申请（通过/拒绝）
- 显示申请详情和用户信息

**权限要求**: `super_admin` 或 `support`

### 8. 系统配置（管理员）
**路径**: `/admin/config`  
**文件**: `frontend/src/pages/admin/Config.vue`  
**功能**:
- 交易配置：最小/最大下单金额、手续费率
- 充值提现配置：最小金额、手续费率
- 系统设置：客服电话、客服微信
- 保存配置

**权限要求**: `super_admin` 或 `support`

---

## 🔧 路由配置

### 路由守卫
**文件**: `frontend/src/router/index.js`

已添加以下逻辑：
1. **登录验证**: 所有页面都需要登录（`requiresAuth: true`）
2. **权限验证**: 管理员页面需要 `super_admin` 或 `support` 角色
3. **游客限制**: 已登录用户不能访问登录/注册页

### 权限检查
```javascript
// 需要管理员权限的页面
if (to.meta.requiresAdmin) {
  const role = userStore.userInfo?.role
  if (role !== 'super_admin' && role !== 'support') {
    next('/')  // 无权限，跳转首页
    return
  }
}
```

---

## 📝 代码规范

### 组件结构
```vue
<template>
  <!-- 页面结构 -->
</template>

<script setup>
/**
 * @file ComponentName.vue
 * @description 组件描述
 * @author AI
 * @date 2025-11-18
 */

// 导入
import { ref, onMounted } from 'vue'

// 响应式数据（带JSDoc注释）
/**
 * 数据说明
 * @type {import('vue').Ref<Type>}
 */
const data = ref(initialValue)

// 方法（带JSDoc注释）
/**
 * 方法说明
 * @async
 * @param {Type} param - 参数说明
 * @returns {Promise<void>}
 */
const method = async (param) => {
  // 实现
}

// 生命周期
onMounted(() => {
  // 初始化
})
</script>

<style scoped>
/* 样式 */
</style>
```

### JSDoc注释
- ✅ 文件头部有描述注释
- ✅ 所有响应式数据有类型注释
- ✅ 所有方法有完整的JSDoc注释
- ✅ 参数和返回值有类型说明

### 命名规范
- **组件**: PascalCase（如 `Orders.vue`）
- **变量/方法**: camelCase（如 `loadOrders`）
- **常量**: UPPER_SNAKE_CASE（如 `API_ENDPOINTS`）
- **CSS类**: kebab-case（如 `.order-item`）

### 导入顺序
1. Vue相关（ref, onMounted等）
2. 第三方库（vant等）
3. 项目内部模块（request, api, helpers）

---

## 🎨 UI设计

### 统一风格
- **主色**: `#667eea` (紫色)
- **辅助色**: `#764ba2` (深紫)
- **成功色**: `#67c23a`
- **危险色**: `#f56c6c`
- **警告色**: `#e6a23c`

### 布局规范
- **页面背景**: `#f7f8fa`
- **卡片圆角**: `8px`
- **阴影**: `box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05)`
- **间距**: 使用`10px`, `12px`, `16px`, `20px`的倍数

### 组件使用
- **导航栏**: `van-nav-bar`（固定+占位）
- **列表**: `van-list`（支持下拉刷新和加载更多）
- **空状态**: `van-empty`
- **表单**: `van-form` + `van-field`
- **按钮**: `van-button`（type: primary/success/danger）

---

## 🧪 测试说明

### 页面访问
所有页面都可以直接通过URL访问（需登录）：

```
http://localhost:5173/orders
http://localhost:5173/bank-cards
http://localhost:5173/notifications
http://localhost:5173/about
http://localhost:5173/admin/users
http://localhost:5173/admin/deposits
http://localhost:5173/admin/withdraws
http://localhost:5173/admin/config
```

### 测试账号

**普通客户** (无管理员权限):
```
用户名: 13800000001
密码: 123456
```

**超级管理员** (有管理员权限):
```
用户名: 13900000000
密码: admin123
```

### 功能测试
1. ✅ 普通用户可以访问：订单、银行卡、通知、关于
2. ✅ 普通用户访问管理员页面会被重定向到首页
3. ✅ 管理员可以访问所有页面
4. ✅ 未登录访问任何页面会跳转到登录页

---

## 📦 依赖说明

所有页面使用的依赖：
- `vue` - Vue 3 Composition API
- `vant` - UI组件库
- `vue-router` - 路由
- `dayjs` - 日期处理（在helpers.js中）
- 项目内部模块：
  - `utils/request.js` - HTTP请求
  - `config/api.js` - API端点配置
  - `utils/helpers.js` - 辅助函数
  - `stores/user.js` - 用户状态管理

---

## ✅ 完成清单

- [x] Orders.vue - 订单页面
- [x] BankCards.vue - 银行卡管理
- [x] Notifications.vue - 系统通知
- [x] About.vue - 关于页面
- [x] admin/Users.vue - 用户管理
- [x] admin/Deposits.vue - 充值管理
- [x] admin/Withdraws.vue - 提现管理
- [x] admin/Config.vue - 系统配置
- [x] 路由配置完成
- [x] 权限验证完成
- [x] JSDoc注释完整
- [x] 代码规范符合要求

---

## 🚀 启动说明

### 前端
```bash
cd frontend
npm run dev
```

### 后端
```bash
cd backend
go run cmd/server/main.go
```

### 访问
```
前端: http://localhost:5173
后端: http://localhost:8080
```

---

**创建时间**: 2025-11-18  
**状态**: ✅ 所有页面已创建并配置完成
