# 测试账号清单

## ✅ 统一响应格式已实施

**所有API现在返回统一格式**: `{data: {...}}`

---

## 👤 可用测试账号

### 🔑 超级管理员 (推荐)
```
手机号: 13900000000
密码:   admin123
角色:   super_admin
权限:   完整系统管理权限
```

### 👨‍💼 普通客户
```
手机号: 13800000001
密码:   123456
角色:   customer
权限:   交易、查看金价、资金管理
```

### 👨‍💼 其他客户
```
手机号: 13800000002
密码:   123456
角色:   customer
```

---

## 🚀 快速登录测试

### 浏览器登录
1. 访问: http://localhost:5175
2. 输入手机号和密码
3. 点击登录

### API测试 (PowerShell)
```powershell
# 测试超级管理员登录
$body = '{"username":"13900000000","password":"admin123"}'
Invoke-RestMethod -Uri http://localhost:8080/api/v1/auth/login `
    -Method POST `
    -Headers @{'Content-Type'='application/json'} `
    -Body $body | ConvertTo-Json -Depth 3

# 测试普通客户登录
$body = '{"username":"13800000001","password":"123456"}'
Invoke-RestMethod -Uri http://localhost:8080/api/v1/auth/login `
    -Method POST `
    -Headers @{'Content-Type'='application/json'} `
    -Body $body | ConvertTo-Json -Depth 3
```

---

## 📊 响应格式示例

### 成功响应
```json
{
  "data": {
    "access_token": "eyJhbGci...",
    "refresh_token": "eyJhbGci...",
    "user": {
      "id": 8,
      "phone": "13900000000",
      "role": "super_admin"
    }
  }
}
```

### 错误响应
```json
{
  "error": "用户名或密码错误"
}
```

---

## 🔧 管理工具

### 查看所有用户
```bash
cd backend
go run scripts/activate_users.go
```

### 创建新的超级管理员
```bash
cd backend
go run scripts/create_admin.go
```

---

## 🎯 前端特性

1. **自动data提取**: 响应拦截器自动提取`data`字段
2. **向后兼容**: 同时支持新旧响应格式
3. **统一错误处理**: 所有API统一错误提示
4. **Token自动刷新**: 401时自动使用refresh_token

---

## 🐛 故障排查

### 登录失败 401
1. 确认手机号和密码正确
2. 检查用户状态是否为`active`
3. 运行 `go run scripts/activate_users.go` 激活用户

### 网络错误
1. 确认后端运行: `netstat -ano | findstr :8080`
2. 确认前端运行: `netstat -ano | findstr :5175`
3. 重启服务

### CORS错误
- 后端已配置CORS，允许 localhost:5173, 5174, 5175

---

## 📝 更新日志

### 2025-11-18
- ✅ 统一后端API响应格式: `{data: {...}}`
- ✅ 创建超级管理员账号: 13900000000/admin123
- ✅ 前端自动提取data字段
- ✅ 简化前端store代码
- ✅ 完善错误处理和提示

---

**登录地址**: http://localhost:5175  
**后端API**: http://localhost:8080  
**推荐账号**: 13900000000 / admin123 (超级管理员)
